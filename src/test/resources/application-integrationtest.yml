kafka:
  security:
    protocol: "plaintext"
  brokers: '${spring.embedded.kafka.brokers}'
  truststore:
    path: "dummt"
  credstore:
    password: "dummy"
  keystore:
    path: "dummy"
  oppgave:
    topic: privat-eessipensjon-oppgave-v1-test
    groupid: eessi-pensjon-group

KAFKA_OPPGAVE_TOPIC: '${kafka.oppgave.topic}'
NAIS_APP_NAME: 'eessi-pensjon-oppgave'

securityTokenService:
  discoveryUrl: http://localhost:${mockServerport}/.well-known/openid-configuration

app.name: ${NAIS_APP_NAME}
NAIS_NAMESPACE: 'test'
GCP_BUCKET_NAME: "dummy_bucket"
EUX_RINA_API_V1_URL: http://localhost:${mockServerport}
securitytokenservice.url: http://someurl
srvusername: someuser
srvpassword: somepass
oppgave.oppgaver.url: http://localhost:${mockServerport}
JOURNALPOST_V1_URL: http://localhost:${mockServerport}
eessipennorg2service_URL: http://localhost:${mockServerport}
OPPGAVE_CLIENT_ID: 32423

#Metrics
management:
  metrics:
    export:
      prometheus:
        enabled: true
  endpoints:
    web:
      exposure:
        include: info, health, metrics, prometheus
  endpoint:
    info:
      show-details: "ALWAYS"

no.nav.orgnummer: 889640782

